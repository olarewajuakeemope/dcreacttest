import React from 'react';

/**
 * Replace with appropriate info on completion
 * @class App
 * @extends {Component}
 */
const Home = ({ children }) => (
  <div>
    <div>
      {children}
    </div>
  </div>
);

export default Home;
